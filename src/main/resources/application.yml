spring:
  datasource:
    url: jdbc:mysql://localhost:3306/inventory_db?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Tokyo
    username: root
    password: MySQL2024!
    driver-class-name: com.mysql.cj.jdbc.Driver

  # （JPA未使用でも将来入れたとき衝突しないように none 推奨）
  jpa:
    hibernate:
      ddl-auto: none

  flyway:
    enabled: true
    locations: classpath:db/migration


  thymeleaf:
    cache: false

server:
  port: 8080
